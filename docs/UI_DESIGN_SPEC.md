# 零一智鉴 · API 真测雷达 — UI 设计规范

> - 版本：1.4
> - 状态：UI 二开实施基线
> - 更新日期：2026-08-26
> - 适用仓库：`01-Yang/zero-one-api-verifier`

## 0. 快速参考

| 项目 | 唯一取值 |
|---|---|
| 中文产品名 | **零一智鉴 · API 真测雷达** |
| 英文产品名 | **ZeroOne · API Verification Platform** |
| 仓库名 | `zero-one-api-verifier` |
| Slogan | **从零到一，让每一个接口有据可鉴！** |
| 正式域名 | `https://01yapi.cc` |
| Canonical 基址 | `https://01yapi.cc` |
| Logo 资产 | 原始主档：`/docs/assets/zero-one-logo-source.png`；Web：`/static/zero-one-logo.png` |
| 默认语言 | `zh-CN` |
| 视觉关键词 | 克制、精密、透明、可信、安静、证据感 |
| 技术约束 | FastAPI + Jinja2 + 原生 CSS/JS；不引入前端框架或构建链 |
| 无障碍目标 | WCAG 2.2 AA |

本规范借鉴 Apple 产品页面的克制、留白、材质层级和排版精度，但不复制 Apple 的资产、字体文件、设备构图、标志性版式、图标或营销文案。零一智鉴必须形成独立、可识别、以“证据链”为核心的视觉语言。

---

## 1. 品牌基础

### 1.1 产品定位

零一智鉴是面向 AI API 使用者、中转站评估者和自托管团队的 API 真测雷达。它以真实探针、官方基线、协议字段、评分与可追溯报告回答三个问题：

1. 接口提供的模型是否与宣称一致；
2. 关键能力是否完整；
3. 响应与流式协议是否合规。

### 1.2 核心价值主张

**不靠口号判断接口，靠公开证据完成真测。**

### 1.3 品牌人格

| 特质 | 我们是 | 我们不是 |
|---|---|---|
| 克制 | 信息优先、装饰适量 | 空洞、冷漠或故意难懂 |
| 严谨 | 给出条件、单位、基线与证据 | 用“绝对”“100%”替代证明 |
| 透明 | 结果可追溯、规则可解释 | 用黑箱结论制造权威感 |
| 精密 | 对齐准确、层级清楚、状态明确 | 视觉堆叠或过度炫技 |
| 可信 | 尊重 API Key、隐私与原始语义 | 暗示未经证明的背书 |

### 1.4 品牌语言

- 默认语气：冷静、直接、具体、基于证据。
- 先说结论，再给依据；错误信息先给可理解摘要，再提供技术详情。
- “检测”“基线”“评分”“报告”“证据”等领域术语沿用上游定义。
- 时间、百分比、Token、延迟、数量必须带单位。
- 产品名、Slogan 不改写、不拆词、不创造其他简称。

推荐用语：

- `开始真测`
- `查看检测报告`
- `本次检测`
- `检测条件`
- `证据详情`
- `基线对比`
- `任务已提交，正在等待执行`
- `检测未完成，请查看失败原因`

禁用用语：

- `一键封神`
- `绝对真实`
- `完美模型`
- `全球领先`
- 无上下文的 `确定`、`好的`、`失败了`

### 1.5 Logo 使用规则

- 唯一主 Logo 为用户提供的黑底白色图形；原始主档固定为 `/docs/assets/zero-one-logo-source.png`。
- 不重绘、不变形、不改色，不添加渐变、描边、发光或其他装饰效果。
- Web 派生文件固定为 `/static/zero-one-logo.png`；只允许裁去黑色安全区并缩放尺寸，不得裁切或改动白色主体。
- 页头使用 `30 × 30px` 黑色方形视窗，同时保留“零一智鉴”文字标识；白色主体在视窗内按约 `78%` 缩放，增加黑色安全区，但不得缩小外部视窗或改动源图。Favicon 使用同一 Web 派生文件，避免出现第二套标志。
- 图形作为产品名旁的装饰时使用空 `alt`；独立出现时必须提供“零一智鉴”替代文本。

---

## 2. 不可变边界

这次二开是视觉与品牌层改造，不是产品核心重写。

### 2.1 必须保持

- `src/relay_detector/` 的检测器、协议客户端、Runner、评分、报告 Schema 和 CLI 行为。
- `data/baselines/` 的官方基线内容与选择逻辑。
- 异步任务状态、并发、持久化、API Key 生命周期和错误语义。
- 排行榜聚合、贝叶斯排序、中位分展示和过滤规则。
- 所有现有页面路由、HTTP 方法、请求字段、响应字段、状态码和下载格式。
- 报告中的分数、Verdict、Detector 状态、字段证据和原始数据。
- 上游 AGPL-3.0-or-later 许可证、来源说明和公开源代码入口。

### 2.2 禁止

- 因换肤重写业务状态机、数据转换或协议请求。
- 前端自行定义新的合格线、评分等级、任务百分比或排名规则。
- 为演示效果伪造检测结果、实时进度、排行榜数据或成功状态。
- 新增后端并未支持的取消、重试、导出、筛选等动作。
- 对整个仓库全局替换 `Veridrop`，从而误改核心代码、协议内容或历史测试。
- 引入 React、Vue、Tailwind、CSS-in-JS 或新的前端打包链。
- 借二开清理无关代码、重命名核心类型或重排基线数据。

### 2.3 可改范围

- Jinja 页面模板的语义结构、品牌文案和视觉层级。
- 原生 CSS 的 Token、布局、响应式、组件状态和动效。
- 原生 JavaScript 中与可访问性或视觉交互直接相关的手术式调整。
- Favicon、Open Graph、robots、sitemap、llms、分享图和页面元数据中的品牌与域名。
- Web 服务层中硬编码的品牌、Canonical、SEO 和下载文件名；不得改变路由或 Payload。
- README、FAQ、贡献说明等面向用户的二开文档，同时保留上游归属。

---

## 3. 设计原则

1. **证据优先**：评分、检测对象、时间、基线、证据和原始字段先于装饰。
2. **一个首要焦点**：每个首屏只设一个主标题、一个主要操作和一个核心结果。
3. **材质表达层级**：实色承载内容；玻璃只承载悬浮、覆盖、导航或短时操作层。
4. **黑白构成骨架**：中性色承担品牌；彩色只承担成功、警告、失败和信息语义。
5. **数据不失真**：视觉强调不得改变排序、比例、阈值、精度或结论含义。
6. **安静的高级感**：依靠排版、留白、边线与节奏，不依靠霓虹、光球或重阴影。
7. **渐进增强**：模糊、透明或动效不可用时，内容仍完整、清晰、可操作。
8. **可追溯改动**：每一处 UI 差异都能追溯到品牌、视觉、响应式、无障碍或 SEO 目标。

---

## 4. 原创视觉母题：Evidence Trace

Evidence Trace 用一条精细的中性色线串联：

`请求 → 探活 → 检测 → 基线 → 评分 → 报告`

它表达“从零到一”和“有据可鉴”，是零一智鉴区别于通用 SaaS 毛玻璃界面的核心母题。

规则：

- 使用 1px 线、4–8px 节点、短标签和明确的真实阶段。
- 只用于流程、报告章节导航、结果追溯或区块分隔。
- 单个视口最多出现一组完整 Trace，避免变成重复纹样。
- 未完成步骤使用空心节点，已完成步骤使用实心节点，错误步骤同时显示图标和文字。
- 后端未提供阶段时，不得根据时间自行推测当前步骤。
- 装饰版本必须标注“示例”，不可伪装成真实检测状态。

---

## 5. Token 架构

采用三层 Token：

`Primitive 原始值 → Semantic 语义值 → Component 组件值`

组件不得直接引用十六进制颜色、任意间距或任意动效时长。当前阶段在单一 CSS 文件中按三层分区即可，不为 Token 单独引入构建工具。

### 5.1 Primitive Tokens

#### 中性色

| Token | 值 | 说明 |
|---|---:|---|
| `neutral-0` | `#FFFFFF` | 纯白 |
| `neutral-50` | `#FAFAF8` | 高光背景 |
| `neutral-100` | `#F6F6F3` | 页面画布 |
| `neutral-200` | `#EFEFEB` | 次级实色表面 |
| `neutral-300` | `#DEDED9` | 强分隔 |
| `neutral-400` | `#B8B8B1` | 禁用边界 |
| `neutral-500` | `#7A7A74` | 弱化文字 |
| `neutral-600` | `#595955` | 次级文字 |
| `neutral-700` | `#3A3A36` | 深色边界 |
| `neutral-800` | `#242422` | 深色表面 |
| `neutral-900` | `#111110` | 主要黑色 |
| `neutral-950` | `#080808` | 深色画布 |

#### 功能色

功能色不可作为大面积品牌色，且必须与文字或图标同时出现。

| Token | 浅色界面 | 深色界面 | 用途 |
|---|---:|---:|---|
| `success` | `#2563EB` | `#60A5FA` | 通过、可用、完成 |
| `good` | `#4F46E5` | `#818CF8` | 良好、次级通过 |
| `warning` | `#7C3AED` | `#C4B5FD` | 警告、部分能力 |
| `danger` | `#6B21A8` | `#D8B4FE` | 失败、错误、危险操作 |
| `info` | `#2459C4` | `#75A7FF` | 中性信息、必要的焦点补色 |

状态色统一使用蓝、紫与蓝紫冷色谱。通过、警告、失败之间必须同时依靠文字与 ✓ / ! / × 图标区分，不能把颜色作为唯一判断依据。

状态组件使用以下固定组合，不在模板中临时拼色：

| 状态 | 主色 | 强调文字 | 浅底 | 边线 |
|---|---:|---:|---:|---:|
| 通过 | `#2563EB` | `#1D4ED8` | `#EFF6FF` | `#BFDBFE` |
| 良好 | `#4F46E5` | `#3730A3` | `#EEF2FF` | `#C7D2FE` |
| 警告 | `#7C3AED` | `#5B21B6` | `#F5F3FF` | `#DDD6FE` |
| 失败 | `#6B21A8` | `#581C87` | `#FAF5FF` | `#E9D5FF` |

#### 尺寸与节奏

- 间距：`4 / 8 / 12 / 16 / 24 / 32 / 48 / 64 / 96 / 128px`
- 控件圆角：`10 / 12px`
- 卡片圆角：`16px`
- 大型面板圆角：`20 / 24px`
- 状态 Pill 圆角：`999px`
- 边线：`1px`
- 页面内容最大宽度：`1280px`
- 报告内容最大宽度：`960px`
- 长文本最大宽度：`720px` 或 `72ch`

#### 阴影

| Token | 值 | 用途 |
|---|---|---|
| `shadow-none` | `none` | 默认静态内容 |
| `shadow-float` | `0 8px 24px rgba(0,0,0,.07)` | 下拉、轻悬浮层 |
| `shadow-modal` | `0 20px 64px rgba(0,0,0,.16)` | 模态层 |

### 5.2 Semantic Tokens

#### 默认浅色主题

| Token | 映射 |
|---|---|
| `canvas` | `neutral-100` |
| `surface-solid` | `neutral-0` |
| `surface-subtle` | `neutral-200` |
| `surface-glass` | `rgba(255,255,255,.68)` |
| `text-primary` | `neutral-950` |
| `text-secondary` | `neutral-600` |
| `text-muted` | `neutral-500` |
| `text-inverse` | `neutral-0` |
| `border-subtle` | `rgba(10,10,10,.10)` |
| `separator` | `rgba(10,10,10,.06)` |
| `hover-overlay` | `rgba(10,10,10,.04)` |
| `pressed-overlay` | `rgba(10,10,10,.08)` |
| `focus-ring` | `neutral-950` |

#### 深色映射（已实施）

深浅色主题由 `html[data-theme]` 选择。首次访问跟随系统偏好，用户手动切换后写入本地偏好；主题脚本在样式表加载前执行，避免明暗闪烁。两套主题使用同一信息架构与组件状态：

| Token | 映射 |
|---|---|
| `canvas` | `neutral-950` |
| `surface-solid` | `#151515` |
| `surface-subtle` | `#1C1C1C` |
| `surface-glass` | `rgba(18,18,18,.72)` |
| `text-primary` | `#F5F5F2` |
| `text-secondary` | `#B3B3AD` |
| `text-muted` | `#898984` |
| `border-subtle` | `rgba(255,255,255,.12)` |
| `separator` | `rgba(255,255,255,.08)` |

### 5.3 Component Tokens

| 组件 Token | 默认值 |
|---|---|
| `button-height` | `44px` |
| `button-height-compact` | `36px` |
| `button-radius` | `18px`（主 CTA）；工具型按钮可保持 `12px` |
| `button-primary-bg` | `text-primary` |
| `button-primary-fg` | `text-inverse` |
| `input-height` | `48px` |
| `input-radius` | `12px` |
| `input-border` | `border-subtle` |
| `card-radius` | `16px` |
| `card-padding-mobile` | `20px` |
| `card-padding-desktop` | `24–32px` |
| `table-row-height` | `52px` |
| `badge-height` | `24–28px` |
| `nav-height-mobile` | `56px` |
| `nav-height-desktop` | `64px` |

---

## 6. 排版系统

### 6.1 字体栈

界面字体：

```css
system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
"PingFang SC", "Noto Sans CJK SC", "Microsoft YaHei", sans-serif
```

代码与协议内容：

```css
ui-monospace, SFMono-Regular, Menlo, Consolas, monospace
```

不随项目分发 SF Pro 字体文件。数值、耗时、Token 数和排名统一使用等宽数字 `tabular-nums`。

### 6.2 字阶

| 样式 | 字号 / 行高 | 字重 | 用途 |
|---|---|---:|---|
| Hero | `clamp(46px, 5.75vw, 69px) / 1.06` | 800 | 首页唯一主标题与 StrokeText 描边填充 |
| Page H1 | `clamp(34px, 4vw, 48px) / 1.10` | 600–650 | 页面标题 |
| H2 | `clamp(28px, 3vw, 36px) / 1.18` | 600 | 大区块标题 |
| H3 | `24px / 1.30` | 600 | 卡片组与报告章节 |
| Body Large | `18px / 1.65` | 400 | 引导说明 |
| Body | `15–16px / 1.65` | 400 | 正文 |
| UI Label | `14px / 1.45` | 500 | 表单、按钮、导航 |
| Meta | `12–13px / 1.50` | 400–500 | 辅助信息 |
| Metric | `clamp(40px, 5vw, 64px) / 1` | 600 | 核心分数或指标 |

规则：

- 中文正文不使用全大写效果或夸张字距。
- 小于 14px 的文字只用于非关键短标签，不承载长文本。
- 通用标题最大字重 650；首页唯一 Hero 可使用 800，但不得把该字重扩散到卡片标题、导航或正文。
- 正文行宽控制在 64–72 个字符，技术明细除外。

---

## 7. 布局系统

| 视口 | 网格 | 页面边距 | 区块纵向间距 |
|---|---|---|---|
| `<640px` | 4 列 | `16px` | `56–72px` |
| `640–1023px` | 8 列 | `24–32px` | `72–96px` |
| `≥1024px` | 12 列 | `32–48px` | `96–128px` |

- 移动端按自然阅读顺序变为单列。
- 除代码块和数据表外，不产生页面级横向滚动。
- 点击目标至少 `44 × 44px`。
- 在 320px CSS 宽度和浏览器 200% 缩放时不得丢失关键功能。
- 标题使用 `clamp()`，不按具体设备型号写断点。
- 若使用 Sticky 元素，必须为系统安全区和键盘焦点留出空间。

层级固定为：

| 层级 | z-index |
|---|---:|
| 页面内容 | 0 |
| Sticky 导航 | 10 |
| Popover / Dropdown | 20 |
| Modal / Drawer | 30 |
| Toast | 40 |

---

## 8. 毛玻璃材质规范

### 8.1 允许位置

- Sticky 顶部导航；
- Popover、Dropdown、Modal、Drawer；
- 浮动操作栏；
- Hero 中面积有限、明确标注的结果预览；
- 有真实背景内容可被感知的悬浮层。

### 8.2 推荐参数

浅色：

```css
background: rgba(255,255,255,.68);
backdrop-filter: blur(24px) saturate(110%);
border: 1px solid rgba(255,255,255,.58);
```

深色：

```css
background: rgba(4,4,4,.72);
backdrop-filter: blur(40px) saturate(120%);
border: 1px solid rgba(255,255,255,.12);
```

### 8.3 使用限制

- 长文本、表格、代码块、表单主体和核心结果面板使用实色或接近实色的表面。
- 一个区域最多两层透明材质，禁止“玻璃套玻璃”。
- 静态卡片主要用边框和背景区分，不默认加阴影或悬浮动画。
- 不使用彩色霓虹描边、大面积模糊光球或彩虹渐变；按钮高光只跟随精细指针，不自动循环。
- 不支持 `backdrop-filter`、强制色彩、高对比或降低透明度环境时，退化为实色表面。
- 玻璃背景必须先满足文字 4.5:1 对比度，再讨论透明度。

---

## 9. 页面规范

### 9.1 首页 `/`

- 首屏顺序固定为：英文产品名 Eyebrow → 中文产品名 H1 → 固定 Slogan → 定义式说明 → 可核验统计 → 唯一主 CTA。主 CTA 进入 Claude 检测；三个协议的并列入口在后续协议区展示，不与首屏主操作争抢层级。
- H1 与 Slogan 使用原生 CSS/JavaScript 实现 StrokeText 式逐字 Wipe + ShinyText：字符使用主题中性实色，不显示紫色或其他彩色描边；`1s` 揭示、字符错峰 `35ms`。Shiny 使用当前主题可读的中性基色与中性高光，按阅读顺序单程 `4s` 从第一个字符扫到最后一个字符，再以逆序 `4s` 返回，完整往返 `8s`，不因换行或 Hover 改变顺序。
- StrokeText 填充在深色主题使用 `#F8FAFC`；浅色主题为保证 WCAG 2.2 AA，使用同等层级的深色语义 Token。此主题适配不得改变字号、排版或文字内容。
- 首页英文 Eyebrow 固定 `15px`，Slogan 使用 `clamp(21px, 2.3vw, 25px)`；二者与 Hero 标题均较上一版放大 `15%`。可核验统计行使用 `18.7px`，较上一版实际渲染字号放大 `10%`。
- 逐字视觉层必须对辅助技术隐藏，并保留一份原文供读屏与无脚本回退；移动端将“· API”视为不可断行簇，禁止中点或 API 缩写被拆行。服务端渲染的原文、定义、统计、CTA 和后续内容默认可见，JavaScript 只能增强装饰。
- 后续顺序保持：开源信任 → 支持协议 → 真测能力 → 检测步骤 → 报告/排行榜入口与结果边界说明；不为了动效改变信息顺序。
- WebGL Threads 是首页独有的氛围层；只在 `/` 挂载。`/claude`、`/openai`、`/gemini`、任务、报告、排行榜和 FAQ 不创建对应 Canvas/WebGL 上下文。WebGL 不可用时保持中性实色背景。
- 不使用“行业第一”“100% 准确”等无法被报告证明的营销结论。

### 9.2 协议检测页 `/claude`、`/openai`、`/gemini`

- 主输入和主提交按钮处于第一视觉层。
- API Key 默认遮蔽，并保留密码管理器、复制粘贴和键盘兼容性。
- Label 在输入框外，不以 Placeholder 代替 Label。
- 帮助文案与错误信息通过语义关系绑定控件。
- 高级参数只呈现上游已有能力；不发明新的模式或开关。
- 提交后沿用现有任务创建、错误恢复和页面跳转行为。

### 9.3 异步任务页 `/r/{job_id}`（运行中）

- 明确展示任务 ID、真实状态、创建/更新时间和后端实际提供的阶段。
- 后端没有百分比时使用不定进度，不计算虚假百分比或倒计时。
- 状态变化使用 `aria-live="polite"` 通知。
- 不新增后端没有的取消或重试按钮。

### 9.4 报告页 `/r/{job_id}`（完成）

阅读顺序固定为：

1. 总体结论与总分；
2. 检测对象、模型、协议、时间与模式；
3. 各检测维度；
4. 证据和协议详情；
5. 基线或比较信息；
6. 可追溯元数据与分享操作。

技术原始数据可以折叠，但不可丢弃或改写。分享、JSON 和 JPG 链接继续使用现有路由与格式。

### 9.5 排行榜 `/leaderboard`、`/leaderboard/{domain}`

- 排名算法、排序依据、过滤规则和显示精度保持不变。
- 数字右对齐并使用等宽数字。
- 状态不能只靠单一颜色表达，必须有文字或图标。
- 移动端可横向滚动表格；只有不损失关键语义时才转成卡片。
- 不隐藏决定排名含义的核心列。
- Sticky 表头可使用实色或轻透明背景，避免多层玻璃叠加。

### 9.6 FAQ `/faq`

- 保持现有问答内容和模式持久化行为。
- 目录、标题和展开控件支持键盘。
- 正文使用长文阅读宽度，不使用玻璃卡片承载长段落。

### 9.7 JPG 报告卡片

- 保留现有 1400 × 1000 输出尺寸、JPG 媒体类型、报告字段和路由契约。
- 替换为零一智鉴品牌、域名和中性设计语言。
- 不因版式省略总分、结论、检测对象、时间或关键证据。

---

## 10. 核心组件

### 10.1 按钮

| 变体 | 样式 | 用途 |
|---|---|---|
| Primary | `#525252` 基础材质、浅色文字、1px 反射边 | 每屏唯一主操作 |
| Secondary | 同材质降低高光强度或使用中性实色 + 1px 边框 | 次级操作 |
| Ghost | 透明底，Hover 使用中性覆盖层 | 导航与低优先级操作 |
| Destructive | 高对比危险状态色 + 明确文字 | 真实破坏性动作 |

- 默认高度 44px，紧凑型不低于 36px。
- 覆盖 default、hover、pressed、focus-visible、loading、disabled。
- Loading 时保持原宽度，避免布局跳动。
- Pill 只用于短状态或筛选，不把所有按钮做成胶囊。
- Specular 半径固定 `18px`，高光直径约 `10px`、衰减约 `40px`，仅在鼠标距控件 `250px` 内跟随；不包裹、不替换现有按钮 DOM。
- 导航开关、主题开关、分页、状态 Chip、FAQ Tab、错误恢复和危险操作不套主 CTA 高光，继续使用各自语义组件。

### 10.2 输入控件

- 默认高度 48px，圆角 12px。
- Focus 使用 2px 高对比前景环 + 2px 背景偏移；必要时才使用信息蓝。
- 错误同时显示边框、图标、文字和 `aria-describedby`。
- URL、模型名、Header 等技术值可使用等宽字体，但不改变上游校验。
- API Key、Token、敏感 Header 继续遵循现有脱敏规则。

### 10.3 卡片与指标

- 移动端内边距 20px，桌面 24–32px。
- 不可点击卡片不提供悬浮抬升。
- 可点击卡片必须具有真实链接或按钮语义。
- 指标必须包含名称、值、单位、时间或上下文。
- 单一总分可以使用环形图；其他维度优先用对齐数值和水平条。

### 10.4 状态标签

- 高度 24–28px，只用于短状态。
- 由文字 + 图标/形状 + 必要功能色共同表达。
- 使用后端已有状态词，不创造与任务状态冲突的新名称。

### 10.5 表格

- 默认行高约 52px。
- 表头 12–13px、字重 500。
- 数字右对齐，文本左对齐，状态居中或按语义对齐。
- Hover 仅做轻微中性底色变化。
- Tooltip 支持键盘和触控，关键解释不可只存在于 Tooltip。

### 10.6 代码与协议详情

- 字号 `13px / 20px`。
- 长行横向滚动，不强制破坏 JSON、Header 或 SSE 格式。
- 复制操作提供明确的成功反馈和 Live Region。
- 不在 UI 层取消核心已有的敏感信息脱敏。

### 10.7 空、错、加载状态

- 空状态解释“为什么为空”，并提供一个明确下一步。
- 错误状态先给用户可理解的摘要，再展开技术详情。
- Skeleton 形状接近真实布局，避免整页闪烁。
- 未知时长使用不定进度，不伪造倒计时。

---

## 11. 数据可视化

- 仅当现有契约提供单一总分时使用环形分数图。
- 若分数契约为 0–100，图表固定为 0–100，不动态缩放放大差异。
- 合格线、等级和权重读取现有结果或基线，不在前端新定义。
- 多系列优先使用不同灰阶、线型和标记，颜色仅作辅助。
- 图表提供文本等价信息、单位、范围和可访问明细。
- 不使用 3D、装饰性仪表盘、粒子动画或无数据依据的趋势。
- 排名、延迟、稳定性等数值保留后端精度，不擅自重新计算。

---

## 12. 动效

| Token | 时长 | 用途 |
|---|---:|---|
| `motion-fast` | `120ms` | 按压、颜色反馈 |
| `motion-base` | `180ms` | Hover、Dropdown |
| `motion-enter` | `260ms` | Modal、Panel 进入 |
| `motion-slow` | `360ms` | 单次页面内容揭示，谨慎使用 |
| `motion-reveal` | `520ms` | 滚动进入视口的一次性渐进出现 |
| `motion-nav` | `700ms` | 顶栏展开/收拢，使用弹性缓动 |

- 主缓动：`cubic-bezier(.2,.8,.2,1)`。
- 优先动画 `opacity` 和 `transform`。
- Hover 位移最多 `-1px`。
- 内容出现动画只执行一次，不随滚动反复触发；JavaScript 失效时内容默认可见。
- 首页标题与 Slogan 允许正向 `4s` + 反向 `4s`、完整往返 `8s` 的中性 ShinyText 循环扫光；这是唯一允许的持续装饰动效，不得扩散到正文或任务界面。
- 禁止其他自动播放视差、无限漂浮、频繁缩放和伪进度动画。
- `prefers-reduced-motion: reduce` 下关闭位移、视差和 Shimmer，只保留即时状态切换。

---

## 13. 无障碍

- 目标 WCAG 2.2 AA。
- 正文对比度至少 4.5:1；大字至少 3:1；控件边界、图形和焦点至少 3:1。
- 所有交互支持键盘，焦点顺序与视觉顺序一致。
- 提供 Skip Link、Landmark、语义化标题层级和正确表格表头。
- 不通过颜色、透明度或动画单独表达状态。
- Modal/Drawer 正确锁定焦点并在关闭后恢复焦点。
- 异步任务、复制成功、表单错误使用适当的 Live Region。
- 支持 `forced-colors`、减少动态效果和无法使用毛玻璃的环境。
- 图标按钮必须有可访问名称。
- 页面标题必须反映协议、任务、报告或排行榜上下文。

---

## 14. SEO、域名与隐私

- 所有 Canonical、Open Graph URL、JSON-LD、Sitemap、robots、llms 和分享图域名统一为 `https://01yapi.cc`。
- 页面标题与描述统一使用“零一智鉴 · API 真测雷达”，同时保留具体协议、模型、域名和报告上下文。
- 不保留上游站点的 Google Analytics ID。上线前只能移除统计，或在获得明确同意后换成零一智鉴自有统计配置。
- 源码入口指向当前 fork，同时显著保留 `Forked from canarybyte/veridrop` 和上游链接。
- 保留 AGPL-3.0-or-later 许可证、无担保声明和网络服务源码获取入口。
- 不暗示上游作者为零一智鉴背书。

---

## 15. 现有前端契约保护

以下 DOM/JavaScript Hook 在实施时必须原样保留，或在同一提交中同步适配并通过页面级测试：

`#detect-form`、`data-endpoint`、`#base_url`、`#api_key`、`#model`、`#model-list`、`.combo-item[data-value]`、`#submit-btn`、`#form-error`、`.nav-toggle`、`#site-nav`、`#share-btn`。

以下请求字段必须保持：

`base_url`、`api_key`、`model`、`mode`、`include_long_context`、`include_long_context_extreme`、`force`。

任务轮询、模型探活、跨协议 Handoff、422 错误恢复、FAQ 模式持久化和分享复制均是功能逻辑，不可当作纯视觉代码删除。

---

## 16. 上游更新与二开保护

上游更新必须遵循[《零一智鉴上游同步与二开保护协议》](./UPSTREAM_SYNC_POLICY.md)。基本规则如下：

- 检测核心、官方基线、任务和排行榜逻辑属于上游优先区，经过测试后持续吸收上游更新。
- 首页、全部页面模板、视觉样式、Favicon 和品牌静态资源属于完全保护区，自动同步不得改变。
- 同时包含业务与品牌的脚本、路由、报告图片、SEO 和元数据文件属于人工合并区，必须逐段审查。
- 每次同步必须从最新下游 `main` 创建 `sync/upstream-*` 分支，并在同步前创建可恢复 Tag。
- 禁止直接重置或强制同步 `main`，禁止使用全局偏向上游的冲突策略覆盖二开内容。
- 自动合并阶段必须先恢复完全保护区；上游在这些文件中的安全、兼容和无障碍修复只能手工迁入。
- 同步 PR 必须通过核心测试、产品身份契约、页面流程、视觉快照和无障碍验证。

每次同步后，下列内容必须保持：

- “零一智鉴 · API 真测雷达”；
- “ZeroOne · API Verification Platform”；
- “从零到一，让每一个接口有据可鉴！”；
- `https://01yapi.cc` 正式域名与 Canonical；
- 首页信息架构、Evidence Trace、Token、毛玻璃边界和响应式体验；
- 当前 Fork 源码入口、上游归属和 AGPL-3.0-or-later 许可证。

“保护 UI”不等于忽略上游对模板或脚本的安全修复。必要修复应适配到零一智鉴现有结构中，并以独立提交说明迁入原因；不得通过复制上游整页来代替局部修复。

---

## 17. Do / Don’t

### Do

- 用留白、排版和对齐建立高级感。
- 用 Evidence Trace 强化“有据可鉴”。
- 让分数、状态、时间和证据形成稳定阅读路径。
- 只在真正悬浮的层级使用玻璃。
- 为每个状态提供文字和可访问反馈。
- 让每一处变更可追溯到明确需求。

### Don’t

- 不把每张卡片都做成半透明玻璃。
- 不使用霓虹、发光描边、粒子背景或大面积彩色渐变。
- 不复制 Apple 的设备构图、官方图标、字体文件或营销句式。
- 不为了“高级感”降低字号、对比度或信息完整性。
- 不伪造进度、实时数据、状态或营销结论。
- 不借换肤重构业务代码、数据模型或 API。
- 不顺手清理与 UI 二开无关的历史代码。

---

## 18. 验收清单

1. 产品名、英文名、Slogan、域名和 Canonical 全站一致。
2. 现有检测输入、任务状态、报告字段、排行榜数据和 API 请求前后保持一致。
3. `src/relay_detector/` 与 `data/baselines/` 相对实施基线不存在功能性差异。
4. 新增颜色、间距、字号、圆角和动效全部来自 Token。
5. 核心页面在 390px、768px、1440px 视口通过视觉检查。
6. 键盘可完成检测提交、查看任务、展开报告、分享和操作排行榜。
7. 自动无障碍扫描没有严重或高优先级问题。
8. 所有成功、警告和失败状态在灰度环境下仍可识别。
9. 关闭模糊、动画或透明效果后，内容仍完整可读。
10. 未支持的动作、指标、进度和营销结论没有被 UI 凭空添加。
11. API Key、Token、Header 等敏感信息继续遵循现有脱敏规则。
12. 上游统计 ID 已移除或替换为明确授权的自有配置。
13. Fork 来源、AGPL 许可证与运行版本源码入口清晰可见。
14. Git 差异中的每项修改均可归因于品牌、视觉、响应式、无障碍、SEO 或必要的 UI 测试。
15. 上游更新只通过独立同步分支和 PR 进入，不直接覆盖 `main`。
16. 自动同步阶段的首页、模板、样式和品牌资产相对同步前下游版本零差异。
17. 上游 UI 层必要修复经过人工迁入、契约测试和视觉审查。
18. 同步后产品名、Slogan、正式域名、Canonical 和 Evidence Trace 保持不变。
19. 首页可见内容包含唯一主 CTA、定义、可核验统计、开源信任、三协议入口、三步检测与结果边界说明。
20. Threads 仅在首页运行；其他路由不建立 WebGL 上下文。
21. 禁用 JavaScript、WebGL 或减少动态效果时，首页内容与主 CTA 仍完整可读可用。
22. 产品契约测试只保护用户可观察内容、路由、归属、可访问性降级与业务 Hook，不绑定像素值、SVG 坐标、私有函数或 JavaScript 实现常量。
